/*
When called in mode 'c', the program returns the list of components as shown above for mode 'l'. Then returns a list of connections that exist between each component, in chronological order.
*/

#ifndef LIST_AND_CONNECTIONS_H
#define LIST_AND_CONNECTIONS_H

// Main function which lists all the components and all their connections. Completes mode "C"
void connection_main(char *template_filename, char *bmp_filename);

#endif 
