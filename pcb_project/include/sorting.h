/*
ALl compare function that have been used for qsort
*/

#ifndef SORTING_H
#define SORTING_H

// These are all taylored compare functions for qsort

int compare(const void *a, const void *b);

int compare_types(const void *a, const void *b);

int compare_ints(const void* a, const void* b);

#endif

